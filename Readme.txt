You need to install graphicsmagic:

sudo apt-get install graphicsmagick

Known bugs
=========
Directories are hard coded into the program
Once the program stops and is run again, it will fail with an error message if the old warhol1 warhol2 etc. directories still exist.